---

apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ $.Release.Namespace }}
  name: component-dashboard-4
data:
  volumes.json: |-
    {{ $.Files.Get "dashboards/cluster/component/volumes.json" | nindent 4 }}
