---

apiVersion: v1
kind: Service
metadata:
  namespace: {{ $.Release.Namespace }}
  name: kube-state
  annotations:
    prometheus.io/scrape: 'true'
  labels:
    app: kube-state
spec:
  ports:
    - name: http-metrics
      targetPort: 8080
      port: 8080
  selector:
    app: kube-state