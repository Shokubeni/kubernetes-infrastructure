---

apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: {{ .Release.Namespace }}
  name: grafana
  annotations:
    nginx.ingress.kubernetes.io/whitelist-source-range: "{{ .Values.general.vpnCidr }}"
    kubernetes.io/ingress.class: nginx
spec:
  tls:
    - secretName: grafana-ingress
      hosts:
        - metrics.{{ .Values.general.domainName }}
  rules:
    - host: metrics.{{ .Values.general.domainName }}
      http:
        paths:
          - path: /
            backend:
              serviceName: grafana
              servicePort: 3000

---

apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  namespace: {{ .Release.Namespace }}
  name: grafana-ingress
spec:
  commonName: metrics.{{ .Values.general.domainName }}
  secretName: grafana-ingress
  issuerRef:
    name: dns-letsencrypt-{{ .Values.general.certIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - metrics.{{ .Values.general.domainName }}

---
