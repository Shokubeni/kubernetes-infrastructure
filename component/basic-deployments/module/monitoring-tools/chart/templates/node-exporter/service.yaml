---

apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Namespace }}
  name: node-exporter
  annotations:
    prometheus.io/scrape: "true"
spec:
  ports:
    - name: metrics
      port: 9100
      protocol: TCP
      targetPort: 9100
  selector:
    app: node-exporter

---
